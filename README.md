# Improving-Monitoring-of-Feature-Usage-Collection
Kotlin parallel file downloader using HTTP Range requests. Falls back to single GET when server doesn't support ranges. JDK 17, Gradle, tests in Docker.
